# Exact runtime versions aligned with NVlabs/alpamayo at the source revision in
# requirements/alpamayo-source.txt. flash-attn and Alpamayo are installed in
# separate phases because flash-attn requires Torch during its build.
accelerate==1.12.0
av==16.0.1
einops==0.8.1
hydra-colorlog==1.2.0
hydra-core==1.3.2
numpy==2.3.5
pandas==2.3.3
physical-ai-av==0.1.0
pillow==12.0.0
pyarrow==21.0.0
scipy==1.16.3
torch==2.8.0
torchvision==0.23.0
transformers==4.57.1
